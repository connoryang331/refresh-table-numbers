{
  "name": "refresh-table-numbers",
  "version": "1.0.0",
  "description": "Refresh table row numbers in Obsidian",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=node --outfile=main.js --external:obsidian --external:fs --external:path"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}